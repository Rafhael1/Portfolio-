  
/* Create something like "if background-image === imageX; change (.title)background linear shit"*/
  

  /*
  (function () {
    var curImgId = 0;
    var numberOfImages = 2; // Change this to the number of background images
    window.setInterval(function () {
        $('header').css('fadeOut','background-image', 
         'url(/backgrounds/image' + curImgId + '.jpg)');
        curImgId = (curImgId + 1) % numberOfImages;
    }, 5000);
})();*/